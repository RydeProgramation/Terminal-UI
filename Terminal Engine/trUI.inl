#include "trUI.h"

#ifndef __TR_UI_INL__
#define __TR_UI_INL__

/*
template<typename FuncType, typename... Args>
void trUserInterface::SetWidget(std::string name, FuncType func, Args... args)
{
	if ((*Widgets)[name])
	{
		std::invoke(func, (*Widgets)[name], std::forward<Args>(args)...);
	}
}*/

#endif